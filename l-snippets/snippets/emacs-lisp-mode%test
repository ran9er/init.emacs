<------modes
emacs-lisp-mode lisp-mode lisp-interaction-mode all
<------keywords
top
<------environment
<------snippet
(defun ${1:FuncName} (${2:Args%? })
  "this func's name is $1"
  (let (${2%(${mirror} $11\n)})
    ${$(insert "hello!")})
    $1=$1
    ${1$(insert user-full-name)}
$4)
